var documenterSearchIndex = {"docs":
[{"category":"type","location":"references/#VTTrac.VTT","page":"References","text":"VTT(z; t=nothing, mask=nothing, zmiss=nothing, fmiss=-999.0, imiss=-999)\n\nSets data for tracking (you need to set parameters separately with setup).\n\nArguments\n\nz::AbstractArray{Float32,3}: Array of image-like data (in dimensions [time, y, x]), with at least 2 time steps. z[i] contains i-th image data.\nt::Union{AbstractVector{Float64}, Nothing}=nothing: Times at which the images are for. Defaults to 0:size(z,1)-1.\nmask::Union{Array{Bool,3}, Nothing}=nothing: Mask to ignore when calculating score (true positions are ignored).\nzmiss::Union{Real, Nothing}=nothing: Missing value used in z.\nfmiss::Real=-999.0: Missing value to be set for Real.\nimiss::Integer=-999: Missing value to be set for Integer.\n\n\n\n\n\n","title":"VTTrac.VTT"},{"category":"function","location":"references/#VTTrac.setup","page":"References","text":"setup(o, nsx, nsy, vxhw, vyhw, [ixhw, iyhw, subgrid, subgrid_gaus,\n    itstep, ntrac, score_method, Sth0, Sth1, vxch, vych,\n    peak_inside, peak_inside_th, Cth, use_init_temp, min_samples])\n\nSetup for tracking.\n\nArguments\n\no::VTT: The object.\nnsx::Integer, nsy::Integer: Subimage x & y sizes (x:1st, y:2nd dim).\nvxhw::Union{Real, Nothing}, vyhw::Union{Real, Nothing}: (either v[xy]hw or i[xy]hw are MANDATORY).   the dimensions along which to perform the computation.   search velocity range half sizes to set i[xy]hw.   Seach at least to cover +-v?hw around the first guess or previous step.   (the result can be outside the range.)\nixhw::Union{Integer, Nothing}, iyhw::Union{Integer, Nothing}: (either v[xy]hw or i[xy]hw are MANDATORY)   Max displacement fro template match (can be set indirecly through v[xy]hw).\nsubgrid::Bool=true: Whether to conduct subgrid tracking.\nsubgrid_gaus::Bool=true: Whether subgrid peak finding is by gaussian.\nitstep::Integer=1: Step of t's used (skip if >1).\nntrac::Integer=2: Max tracking times from initial loc.\nscore_method::String=\"xcor\": \"xcor\" for cross-correlation, \"ncov\" for normalized covariance.\nSth0::Real=0.8: The minimum score required for the 1st tracking.\nSth1::Real=0.7: The minimum score required for subsequent tracking.\nvxch::Union{Real, Nothing}=nothing: If non-nothing, the max tolerant vx   change between two consecutive tracking. Screening by velocity change is only   active when both vxch and vych are set (both non-nothing and positive).\nvych::Union{Real, Nothing}=nothing: If non-nothing, the max tolerant vy   change between two consecutive tracking. See vxch.\npeak_inside_th::Union{Real, Nothing}=nothing: If non-nothing, an initial template is used only when   it is peaked (max or min) inside, exceeding the max or min along the sides by the ratio specified by its value.\nCth::Union{Real, Nothing}=nothing: If non-nothing, an initial template is used only when    it has a difference in max and min greater than its value.\nmin_samples::Integer=1: Minimum number of visible values to calculate score when chk_mask is true.\n\n\n\n\n\n","title":"VTTrac.setup"},{"category":"function","location":"references/#VTTrac.set_ixyhw_from_v","page":"References","text":"set_ixyhw_from_v(o, vxhw, vyhw)\n\nSets the tracking parameters i[xy]hw from velocities (v[xy]hw).\n\nArguments\n\no::VTT: The object.\nvxhw::Real: The range over which vx is searched around initial guess.\nvyhw::Real: The range over which vy is searched around initial guess.\n\n\n\n\n\n","title":"VTTrac.set_ixyhw_from_v"},{"category":"function","location":"references/#VTTrac.set_ixyhw_directly","page":"References","text":"set_ixyhw_directly(o, ixhw, iyhw)\n\nSets the tracking parameters i[xy]hw.\n\nArguments\n\no::VTT: The object.\nixhw::Integer: The range over which next x is searched around initial guess.\niyhw::Integer: The range over which next y is searched around initial guess.\n\n\n\n\n\n","title":"VTTrac.set_ixyhw_directly"},{"category":"function","location":"references/#VTTrac.trac","page":"References","text":"trac(o, tid, x, y[, vxg, vyg, out_subimage, out_score_ary])\n\nConduct tracking.\n\nArguments\n\no::VTT: The tracking object.\ntid::Array{Integer,Any}: Tracking initial time indices.\nx::Array{Float64,Any}: Tracking initial template-center x location (index-based; non-integer for subgrid).\ny::Array{Float64,Any}: Tracking initial template-center y location (index-based; non-integer for subgrid).\nvxg::Array{Float64,Any}=nothing: First guess of vx (to search around it). Can be 0.\nvyg::Array{Float64,Any}=nothing: First guess of vy (to search around it). Can be 0.\nout_subimage::Bool=false: Whether output subimages.\nout_score_ary::Bool=false: Whether output score arrays.\nto_missing::Bool=true: Whether output missing values as missing.\n\nReturns\n\ncount::Vector{Integer}: [len] The number of valid trajectory points for each initial   template, including the initial one (so it ranges 0:ntrac+1; e.g. ntrac+1 means every   step succeeded).\nstatus::Vector{Integer}: [len] The reason tracking stopped for each initial template   (0 if it completed ntrac steps without stopping early). One of:\n0: completed successfully (or not yet stopped)\n1/5: the start/end tid for a step was out of range\n2: the template subimage could not be read (out of bounds, or missing/masked data)\n3: the template subimage failed the Cth contrast check\n4: the template subimage failed the chk_peak_inside check\n6: the sliding score could not be computed (out of bounds, or missing/masked data)\n7: no valid score peak, or the peak was on the edge of the search window\n8: the peak score was below Sth0 (1st step) or Sth1 (subsequent steps)\n9: the velocity change along the trajectory exceeded vxch/vych\ntid::Matrix{Float64}: [ntrac+1, len] time index of the trajectories (tid0 and subsequent ones).\nx::Matrix{Float64}: [ntrac+1, len] x locations of the trajectories (x0 and derived ones).\ny::Matrix{Float64}: [ntrac+1, len] y locations of trajectories (x0 and derived ones).\nvx::Matrix{Float64}: [ntrac, len] Derived x-velocity.\nvy::Matrix{Float64}: [ntrac, len] Derived y-velocity.\nscore::Matrix{Float64}: [ntrac, len] Scores along the trajectory (max values, possibly at subgrid).\nzss::Array{Float32,4}: [ntrac+1, nsy, nsx, len] optional, if non-nothing   (Diagnosis output if wanted) The subimages along the track.\nscore_ary::Array{Float64,4}: [ntrac, 2iyhw+1, 2ixhw+1, len] optional, if non-nothing   (Diagnosis output if wanted) The entire scores.\n\n\n\n\n\n","title":"VTTrac.trac"},{"category":"section","location":"howtouse/#How-to-Use","page":"How to Use","text":"","title":"How to Use"},{"category":"section","location":"howtouse/#Overview","page":"How to Use","text":"Using VTTrac generally follows five steps:\n\nWrap your image time series in a VTT object.\nConfigure tracking parameters with setup.\nChoose the initial time/position of each template you want to track.\nRun trac.\nInterpret the returned trajectories, using count/status to tell which points succeeded.","title":"Overview"},{"category":"section","location":"howtouse/#1.-Prepare-the-data","page":"How to Use","text":"z must be a 3-D Float32 array in [time, y, x] order, with at least 2 time steps. t is optional and defaults to 0:size(z,1)-1; it only needs to be supplied if your time steps are irregularly spaced or you want physically-dimensional velocities.\n\nusing VTTrac\n\nnt, ny, nx = 10, 100, 100\ntax = Vector{Float64}([0:nt-1;])\nxax = [0:nx-1;]\nyax = [0:ny-1;]\nxg = permutedims(repeat(xax', outer=(length(yax),1,length(tax))), (3,1,2))\nyg = permutedims(repeat(yax, outer=(1,length(xax),length(tax))), (3,1,2))\ntg = repeat(tax, outer=(1, length(yax), length(xax)))\nk = 2pi/10\ncx, cy = 1.2, 1.2\nz = sin.(k*(xg .- cx*tg)) .* cos.(k*(yg .- cy*tg))\nz = Array{Float32,3}(z) # a wave pattern drifting at (vx, vy) = (cx, cy)","title":"1. Prepare the data"},{"category":"section","location":"howtouse/#2.-Create-the-VTT-object","page":"How to Use","text":"vtt = VTT(z, t=tax)\n\nIf some pixels should never be used for scoring (e.g. sensor gaps, quality-flagged pixels), pass a Bool array of the same size as z via mask=..., where true means \"ignore this pixel\". See Masking invalid pixels below.","title":"2. Create the VTT object"},{"category":"section","location":"howtouse/#3.-Configure-tracking-with-setup","page":"How to Use","text":"setup(vtt, 5, 5; vxhw=1.8, vyhw=1.8, ntrac=5, subgrid=true, score_method=\"xcor\")\n\nnsx, nsy (positional): the template sub-image size.\nEither vxhw/vyhw (an expected velocity range) or ixhw/iyhw (a pixel search half-width) is mandatory — they determine how far around the first guess (or the previous step's result) the next match is searched for.\nntrac: how many times each initial template is tracked forward (or backward, see itstep below). Use ntrac >= 2 if you want velocity-change screening (vxch/vych).\nscore_method: \"xcor\" (cross-correlation, the default) or \"ncov\" (normalized covariance) — see Available scoring methods on the Home page.\nitstep: the index step between tracked frames; negative values track backward in time.\nsubgrid, subgrid_gaus: whether/how to refine the matched position to sub-pixel accuracy (elliptic-paraboloid by default, Gaussian if subgrid_gaus=true).\n\nSee setup for the full parameter list, including the result-screening options covered in Screening options below.","title":"3. Configure tracking with setup"},{"category":"section","location":"howtouse/#4.-Choose-initial-points-and-track","page":"How to Use","text":"tid0, x0, y0 locate each template's starting time index and (x, y) position (index-based; non-integer positions are allowed and are read out via bilinear interpolation). They must share the same shape — any shape works, not just vectors.\n\nn = 6\ntid0 = ones(Int, n)\nx0 = 1 .+ [0:n-1;]*2.5 .+ 7.5\ny0 = 1 .+ [0:n-1;]*1.0 .+ 10.5\n\ncount, status, tid, x, y, vx, vy, score, zss, score_ary = trac(vtt, tid0, x0, y0)\n\nout_subimage=true/out_score_ary=true additionally return the template sub-images and full sliding-score arrays along the trajectory, which is useful for diagnosing why a particular point stopped tracking (see zss, score_ary in trac).","title":"4. Choose initial points and track"},{"category":"section","location":"howtouse/#5.-Interpret-the-results","page":"How to Use","text":"Every output's first dimension (or, for count/status, the whole array) is shaped like tid0/x0/y0. tid, x, y have ntrac+1 rows — the initial point plus one row per successful step; vx, vy, score have ntrac rows, one per step.\n\ncount[m]: how many trajectory points (including the initial one) are valid for the m-th template — ntrac+1 means every step succeeded.\nstatus[m]: why tracking stopped for the m-th template (0 if it never stopped early). See the table below.\nBy default (to_missing=true), positions past count[m] are filled with missing rather than the raw sentinel values (vtt.fmiss/vtt.imiss).\n\nstatus Meaning\n0 Completed successfully (or the step hasn't been reached yet)\n1 / 5 The start/end time index for this step was out of range\n2 The template sub-image could not be read (out of the image bounds, or missing/masked data)\n3 The template sub-image failed the Cth contrast check\n4 The template sub-image failed the peak_inside_th check\n6 The sliding score could not be computed (out of bounds, or missing/masked data)\n7 No valid score peak was found, or the peak fell on the edge of the search window\n8 The peak score was below Sth0 (1st step) or Sth1 (subsequent steps)\n9 The velocity change along the trajectory exceeded vxch/vych","title":"5. Interpret the results"},{"category":"section","location":"howtouse/#Masking-invalid-pixels","page":"How to Use","text":"Pass mask (same shape as z, true = ignore) when constructing VTT to skip untrustworthy pixels during scoring:\n\nmask = falses(size(z))\nmask[:, 1:5, :] .= true # e.g., the top 5 rows are known to be bad\n\nvtt = VTT(z, t=tax, mask=mask)\nsetup(vtt, 5, 5; vxhw=1.8, vyhw=1.8, ntrac=5, min_samples=10)\n\nmin_samples sets how many jointly-unmasked pixels are required (between the template and the candidate window) before a score is computed there at all; positions with fewer are treated as undefined, the same as status = 6.","title":"Masking invalid pixels"},{"category":"section","location":"howtouse/#Screening-options","page":"How to Use","text":"Sth0, Sth1 (defaults 0.8, 0.7): minimum score required for the 1st and subsequent steps, respectively.\nvxch, vych: if both are set (positive), a step is rejected when the velocity changes by more than this amount from the previous step (status = 9); this needs ntrac >= 2 to have any effect.\npeak_inside_th: if set, an initial template is only used when it has a prominent interior peak or trough (rejects flat/edge-dominated templates, status = 4).\nCth: if set, an initial template is only used when its max−min exceeds this value (rejects low-contrast templates, status = 3).\nuse_init_temp: if true, the template read out at the very first step is reused for every subsequent step, instead of re-reading a fresh template around the current position at each step.","title":"Screening options"},{"category":"section","location":"howtouse/#Tracking-backward-in-time","page":"How to Use","text":"Set itstep to a negative value in setup to track backward along the time axis from tid0.","title":"Tracking backward in time"},{"category":"section","location":"#VTTrac","page":"Home","text":"(Image: Stable) (Image: Dev) (Image: Build Status) (Image: Coverage)\n\nDocumentation for VTTrac.","title":"VTTrac"},{"category":"section","location":"#VTTrac:-Velocimetry-by-Template-Tracking","page":"Home","text":"This library provides the julia-language implementation for VTTrac (https://github.com/thorinouchi/VTTrac). It does not use module variables, so it should be good for parallel execution.\n\nThe algorithm used in this library is the simple template matching of PIV (particle image velocimetry) for monochromatic image-like data, but the matching is conducted multiple times in a Lagrangian manner as in PTV (particle tracking velocimetry) over a number of times specified by the parameter named ntrac. The default scoring method for template matching is the cross correlation coefficient, as in the basic PIV. Both forward and backward tracking is available. Use the parameter itstep; tracking is backward along time sequence, if it is negative.","title":"VTTrac: Velocimetry by Template Tracking"},{"category":"section","location":"#Available-scoring-methods","page":"Home","text":"XCOR: cross-correlation, cov(x',y')/sig(x)/sig(y), where x represents the template sub-image and y represents the target sub-image that are slided.\nNCOV: normalized covariance, cov(x',y')/sig(x)^2: covariance normalized the variance of the template sub-image x.","title":"Available scoring methods"},{"category":"section","location":"#Screening","page":"Home","text":"A check (result screening) based on velocity change along trajectory is available (by using the threshold parameter named vxch and vych), so it is recommended to always set ntrac >= 2. Further screening is available for initial templates (e.g., in terms of the complexity and contrast) and the quality of the results (e.g., score threshold); see the source code.","title":"Screening"},{"category":"section","location":"#Dimensions","page":"Home","text":"Spatial coordinates are based on array indices, with the distance between adjacent grid points always being 1, so they are non-dimensional. The velocities are based on non-dimensional spacial displacement over time difference, where, time can either be dimensional or non-dimensional.","title":"Dimensions"},{"category":"section","location":"#References","page":"Home","text":"VTTrac by Takeshi Horinouchi: https://github.com/thorinouchi/VTTrac\npyVTTrac by Taiga Tsukada: https://github.com/tsukada-cs/pyVTTrac","title":"References"}]
}
